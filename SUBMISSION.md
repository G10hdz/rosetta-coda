# GPT-5.6 Sol submission kit

Do not post until `artifacts/demo/sol-hypothesis.json` has been generated by a
real GPT-5.6 Sol API call and inspected.

## Reply draft

> Not a whale translator. Rosetta Coda runs deterministic stats first; GPT-5.6
> Sol then proposes one falsifiable phonological hypothesis over frozen
> evidence—with exact JSON refs, alternatives, uncertainty & falsifiers. No
> semantic claims. https://github.com/G10hdz/rosetta-coda

Attach the video directly to the reply instead of linking to it.

## 30-second recording

1. **0–4s — Hook:** README title and the line “not a translator.” Overlay:
   `AI does careful science, not vibes.`
2. **4–10s — Frozen evidence:** show the passing gate, 628 observations, and
   the mixed-model coefficient from
   `artifacts/gates/spec-004-duration-gate.json`.
3. **10–18s — Sol boundary:** run `uv run python -m scripts.run_sol_demo`.
   Cut the wait, but keep the command and successful output visible.
4. **18–28s — Result:** show `model`, `source_artifact_sha256`, `claim`,
   `evidence_refs`, `alternatives`, and `falsifiers` from the generated JSON.
5. **28–30s — Close:** overlay `No meaning claims. Every claim can lose.`

Use a 16:9 crop, large terminal text, no narration unless the audio is clean,
and keep the cursor moving only when it directs attention.

## Recording commands

```bash
jq '{state, cohort_flow, mixed_model}' artifacts/gates/spec-004-duration-gate.json
uv run python -m scripts.run_sol_demo
jq '{model, source_artifact_sha256, hypothesis: {claim: .hypothesis.claim, evidence_refs: .hypothesis.evidence_refs, alternatives: .hypothesis.alternatives, falsifiers: .hypothesis.falsifiers}}' artifacts/demo/sol-hypothesis.json
```
